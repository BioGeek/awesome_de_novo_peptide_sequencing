.ipynb_checkpoints/
_site/
.quarto/
_freeze/
plots/
citation_audit.csv

/.quarto/
__pycache__/
**/*.quarto_ipynb
**/*.quarto_ipynb_*
version_audit.csv
candidates.csv
pages/
author_id_audit.csv

# OpenAlex response cache for build_affiliations.py (institutions are static,
# so this has no expiry; safe to delete, just slower on the next run).
.cache/
affiliation_audit.csv

# Quarto writes <name>_files/ next to any document rendered OUTSIDE the project
# render list (a scratch probe, say). One such directory, 885 KB of bundled
# Bootstrap and JS, was committed by a `git add -A` after exactly that.
/*_files/
